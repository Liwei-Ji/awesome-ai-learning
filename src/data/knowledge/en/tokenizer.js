export default {
  qs: ['What exactly is a token?', 'Why do rare words get split up?', 'Do Chinese and English use the same number of tokens?', 'Why does token count affect cost?'],
  kb: [
    [['token', 'what is', 'definition', 'unit', 'smallest'], 'A <b>token</b> is the model’s <b>smallest unit of text</b>—roughly one Chinese character, one common English word, or a small piece of a word (a subword). The model doesn’t see “a sentence,” it sees “a string of tokens.”'],
    [['tokenizer', 'what does it do', 'function', 'component'], 'A <b>tokenizer</b> splits text into tokens and turns each token into a <b>number (ID)</b>. It’s the <b>first gate</b> text passes through into the model.'],
    [['why', 'how', 'how it splits', 'flow', 'process'], 'Computers don’t understand text, only numbers. So the tokenizer first <b>splits the sentence into tokens → converts them to IDs</b> so the model can read it. The flow: <b>text → tokens → numbers</b>.'],
    [['split', 'subword', 'rare', 'long word', 'why split'], 'Common words are usually <b>one token</b>; rare or very long words that aren’t in the vocabulary get split into several <b>subwords</b>. That way even unseen words can be assembled.'],
    [['chinese', 'english', 'difference', 'which more', 'language', 'same'], 'Splitting differs, so counts differ. <b>English</b> common words are often a single token; <b>Chinese</b> is often one token per character or more. So <b>for the same meaning, Chinese usually costs more tokens</b>.'],
    [['code', 'symbols', 'space', 'newline', 'punctuation'], 'Code, punctuation, and <b>spaces and newlines</b> all count as tokens too. Code has lots of symbols and indentation, so it often splits into more tokens than you’d expect.'],
    [['billing', 'cost', 'price', 'money', 'api', 'expensive', 'save'], 'Most APIs <b>bill by token count</b> (input + output combined). So <b>how you write directly affects cost</b>—rambling on literally spends money.'],
    [['misconception', 'not a character', 'letter', 'not equal', 'one char'], 'Common misconception: that <b>1 token = 1 character or 1 letter</b>. In fact a token is a <b>variable-size chunk</b>—it might be half a word, a whole word, or just a space.'],
    [['vocabulary', 'vocab', 'dictionary', 'recognizes'], 'The <b>vocabulary</b> is the full list of tokens a tokenizer recognizes, fixed before training. For words not in the vocabulary, it <b>assembles them from subwords</b>.'],
    [['embedding', 'vector', 'relationship', 'next', 'after'], 'Tokenizing only splits and numbers the text. <b>Next</b> comes <b>Embedding</b>: turning each token’s ID into a meaningful <b>vector</b>, where the model starts to “understand.”'],
    [['context', 'context window', 'window', 'limit', 'memory', 'at once'], 'How much a model can read “at once” is measured in <b>tokens</b>—that’s the <b>context window</b> limit. Too many tokens exceed the limit and get truncated.'],
    [['bpe', 'algorithm', 'how decided', 'how trained', 'merge', 'rules'], 'Tokenizers aren’t split by hand. Algorithms like <b>BPE</b> gather statistics over <b>lots of text</b> and progressively merge the most frequently co-occurring characters into tokens—more common combinations are more likely to be a single chunk.'],
    [['emoji', 'special character', 'garbled', 'foreign'], 'One emoji or rare symbol may be <b>several byte-tokens</b> to the model. It looks like a single character but can quietly eat several tokens.'],
    [['per token', 'generate', 'one at a time', 'how produced', 'output'], 'The model processes and generates <b>token by token</b>: it computes the most likely next token, appends it, then computes the next. So it doesn’t “think out a whole sentence,” it produces one chunk at a time.'],
  ],
  notes: [
    'A token is the model’s basic unit—not “a character” or “a letter”; its size varies.',
    'Common words are one token; rare or very long words split into subwords.',
    'The same sentence yields very different token counts in Chinese, English, and code.',
    'Order: first tokenize (split & number), then embed (turn into meaningful vectors).',
    'Token count is limited by the context window and is often the unit of API billing—directly tied to cost.',
  ],
};
