export default {
  qs: ['What is AI system integration?', 'How do all these concepts come together into one app?', 'What parts make up an AI product?', 'What can I build now that I’ve learned all this?'],
  kb: [
    [['what is', 'definition', 'define', 'system integration', 'integration', 'integrate'], '<b>AI system integration</b> means taking each piece of technology you’ve learned and <b>stringing it into one complete pipeline</b>: starting from data, through training, inference, retrieval, and tools, until it becomes an <b>application</b> people can actually use. A single technology isn’t a product, only connecting them together makes one.'],
    [['pipeline', 'flow', 'stages', 'sequence', 'data flow', 'how it works', 'chain'], 'A <b>pipeline</b> is the <b>series of processing stages</b> that data flows through, like a factory conveyor belt: the output of one stage is the input to the next. The most basic AI pipeline is <b>data → model → prediction → decision</b>.'],
    [['bottom up', 'stack', 'layers', 'bottom', 'top', 'architecture', 'how to assemble', 'put together', 'combine'], 'It stacks from the bottom up: <b>data</b> sits at the base → ML/DL train it into a <b>neural network</b> → the Transformer lets it read language, becoming an <b>LLM</b> → then retrieval, memory, tools, and Agents are added → and at the very top is the <b>application</b> the user touches.'],
    [['why', 'need', 'purpose', 'point', 'reason', 'what for'], 'Because <b>no single technology can be a product on its own</b>. A model can compute, but it can’t fetch its own data, can’t look up the latest information, and can’t take action, it takes <b>connecting the pieces</b> so they cover for each other to make a system that truly works.'],
    [['application', 'app', 'product', 'user', 'final', 'end product'], 'An <b>application</b> is the <b>very top</b> of the pipeline, the product users actually interact with, such as a customer-support bot or a writing assistant. Users never see the model or data underneath; they just feel that “it got the job done for me.”'],
    [['misconception', 'myth', 'not', 'assume', 'just a model', 'equals'], 'A common misconception: thinking “an AI app = one model.” In reality the model is just <b>one segment</b>; a real product is <b>data, model, retrieval, memory, tools, and interface</b> combined, and the model is often the easiest part to swap out.'],
    [['relationship', 'earlier', 'chapters', 'each chapter', 'review', 'summary', 'tie together'], 'Every earlier chapter is <b>one segment</b> of this pipeline: data is the nutrient, ML/DL are the learning methods, neural networks and the Transformer are the model’s skeleton, the LLM is the brain, and RAG/memory/MCP/Agents are its hands and feet. Integration is connecting them into a single line.'],
    [['how to design', 'steps', 'get started', 'how to do', 'how', 'design', 'build', 'create'], 'Designing an AI app roughly goes: <b>①first get clear on what problem you’re solving ②prepare your data ③pick a good-enough model ④add retrieval or tools so it connects to real information ⑤build an interface ⑥keep validating and improving</b>. Work backward from the need to decide which segments you need.'],
    [['replace', 'upgrade', 'module', 'modular', 'swap', 'flexible', 'swappable'], 'This pipeline is <b>modular</b>: each layer can be <b>swapped or upgraded</b> on its own, a stronger model, better data, an added retrieval step, without redoing everything. That’s why understanding the whole picture matters: it tells you which segment to change.'],
    [['example', 'sample', 'real', 'customer support', 'for instance', 'how it’s built', 'break down'], 'Take a <b>customer-support bot</b>: the user’s question is first split into tokens → RAG searches company documents for relevant passages → the passages plus the question go to the LLM to generate an answer → to check an order it connects to a database via a tool or MCP → the result is composed into a reply → and it shows up in the chat interface. <b>One answer travels through several pipeline segments.</b>'],
    [['rag', 'agent', 'mcp', 'tools', 'retrieval', 'roles', 'division of labor', 'each one'], 'In an integration, each piece has its job: <b>RAG</b> lets the model look up the right data, <b>MCP</b> is the universal socket that plugs in tools and data, and an <b>Agent</b> is the brain that plans and decides what to do first. They all surround the LLM, filling in what it can’t do alone.'],
    [['downside', 'challenge', 'hard', 'caution', 'risk', 'difficult', 'watch out'], 'The hard part of integration isn’t any single technology, it’s <b>connecting the segments well, and keeping it fast, stable, and cheap</b>: data must be clean, cost controlled, latency low, and output verified. The longer the chain, the more careful you must be, because a fault in one segment drags down the whole pipeline.'],
  ],
  notes: [
    'Most AI products are a combination of a few segments of this pipeline, not a single model.',
    'The pipeline runs bottom-up: data at the base, application at the top, and every layer can be swapped or upgraded.',
    'Every earlier chapter is one segment of this pipeline, integration is connecting them into a single line.',
    'The starting point for designing an AI app is “what problem am I solving,” then working backward to which segments you need.',
    'The longer the chain, the more care it takes: a fault in any one segment affects the pipeline’s final result.',
  ],
};
