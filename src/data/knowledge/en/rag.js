export default {
  qs: ['What is RAG?', 'Why does RAG reduce hallucinations?', 'How is RAG different from fine-tuning?', 'How does RAG work?'],
  kb: [
    [['what is', 'definition', 'define', 'rag', 'retrieval augmented', 'retrieval-augmented generation'], '<b>RAG (Retrieval-Augmented Generation)</b> means letting the AI <b>look things up before it answers</b>: first it finds the passages in a knowledge base that relate to your question, then it hands those passages plus the question to the LLM so it can <b>generate an answer based on the data</b>. In one line: <b>retrieve first, then generate</b>.'],
    [['why', 'need', 'purpose', 'forgets', 'misremembers', 'outdated', 'latest', 'private'], 'Because an <b>LLM’s knowledge is frozen at training time</b>, it <b>may not remember, and can misremember</b>, and it has no idea about things inside your company or events that just happened. RAG lets it look up external data on the fly, so it can answer <b>private or up-to-date</b> questions, without retraining the model.'],
    [['how it works', 'process', 'flow', 'steps', 'order', 'mechanism', 'pipeline'], 'The flow is: <b>①</b> split the knowledge base documents into chunks, turn them into vectors, and store them → <b>②</b> when you ask, turn the question into a vector too → <b>③</b> <b>compare similarity</b> to find the few most relevant passages → <b>④</b> drop those passages into the prompt so the <b>LLM answers based on them</b>.'],
    [['hallucination', 'reduce', 'made up', 'fabricate', 'accurate', 'reliable', 'guessing'], 'RAG <b>reduces hallucinations</b> because the answer is <b>grounded in data it actually found</b> rather than guessed from a fuzzy memory. It can even <b>cite sources</b> so you can click through and verify, but note: <b>if the retrieval is wrong, the answer will be wrong too</b>. RAG only lowers hallucinations, it doesn’t eliminate them.'],
    [['retrieval', 'retrieve', 'similarity', 'vector', 'how does it find', 'find data', 'matching'], '<b>Retrieval</b> relies on <b>vector similarity</b>: both the question and the documents are turned into vectors (embeddings), then it checks which passages’ vectors are <b>closest to the question</b>, those are the most relevant in meaning. So it finds content that is <b>close in meaning</b>, not an exact word-for-word match.'],
    [['chunking', 'chunk', 'split', 'segment', 'passage', 'break up'], 'You can’t drop a whole document in at once, it’s first <b>split into small chunks (chunking)</b> and then stored. Chunks that are too big pull in irrelevant content and won’t fit the context; chunks that are too small <b>cut the meaning apart</b>. <b>Your chunking strategy and retrieval quality directly decide how good the RAG is</b>.'],
    [['vector database', 'vector', 'database', 'store', 'knowledge base', 'db'], 'The chunks are turned into vectors and stored in a <b>vector database</b>. It does one thing well: <b>quickly find the passages whose vectors are closest to the query vector</b>. This is RAG’s “library”, it decides what the AI can find, and how fast.'],
    [['fine-tuning', 'fine tuning', 'finetune', 'difference', 'how is it different', 'which is better', 'compare', 'versus'], 'They have different goals: <b>fine-tuning</b> writes knowledge <b>into the model’s weights</b> (changing what it inherently knows); <b>RAG</b> lets the model <b>look up external data on the fly</b> (without changing the model). Use fine-tuning to teach <b>tone and format</b>; use RAG to plug in <b>facts that change often</b>, and you can use both together.'],
    [['embedding', 'relationship', 'related', 'which chapter', 'foundation', 'builds on'], 'RAG <b>is built on top of Embeddings</b>: whether storing a document or matching a question, the text must first be turned into a <b>vector</b> before similarity can be computed. You could say Embeddings provide the “coordinates of meaning,” and RAG uses those coordinates to <b>find the most relevant data</b>.'],
    [['application', 'use case', 'where used', 'example', 'scenario', 'where'], 'Common uses: <b>internal company Q&A</b> (searching company documents and policies), <b>customer-service bots</b> (answering from a product manual), <b>document assistants</b> (asking about the contents of a PDF), and <b>legal or medical lookup</b>, anywhere you need answers <b>based on specific data and with sources attached</b> is a good fit.'],
    [['misconception', 'not', 'remember', 'learn', 'train', 'same as', 'search engine'], 'A common misconception: RAG <b>doesn’t “teach” the data to the model</b>, it won’t remember next time; it <b>looks things up fresh every time</b>. It’s also <b>not just keyword search</b>, it uses <b>semantic similarity</b> to find relevant content, then lets the LLM digest it into a fluent answer.'],
    [['pros', 'cons', 'limits', 'limitations', 'benefits', 'drawbacks', 'problems'], 'Upsides: <b>knowledge can be updated instantly, sources can be cited, and it costs far less than retraining</b>. Limits: <b>if retrieval is off, the answer goes wrong with it</b>, passages take up context space, and the system is more complex than plain chat. Quality depends heavily on <b>how well the data is organized and chunked</b>.'],
  ],
  notes: [
    'RAG = retrieve first, then generate: find relevant passages in a knowledge base, then let the LLM answer based on them.',
    'RAG can answer private or up-to-date questions, without retraining the model.',
    'Retrieval relies on vector similarity (embeddings), it finds close meaning, not exact wording.',
    'Chunking and retrieval quality directly decide how good the RAG is.',
    'RAG only lowers hallucinations, it doesn’t eliminate them: retrieve the wrong passage and the model still answers wrong.',
    'Division of labor with fine-tuning: use fine-tuning for tone and format, RAG for facts that change often.',
  ],
};
