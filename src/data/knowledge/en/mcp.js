export default {
  qs: ['What exactly is MCP?', 'How is MCP different from an API?', 'How is MCP different from RAG?', 'Why do we need MCP?'],
  kb: [
    [['what is', 'definition', 'define', 'mcp', 'model context protocol', 'full name', 'meaning'], 'MCP stands for <b>Model Context Protocol</b>, an <b>open standard</b> that lets AI use one common way to read data and call external tools—instead of wiring up a separate connection for every single tool.'],
    [['why', 'need', 'solve', 'problem', 'benefit', 'purpose', 'point of it'], 'Without MCP, connecting AI to ten tools means writing ten different connections—<b>messy and hard to maintain</b>. MCP turns that into <b>one unified spec</b>: a tool exposes a single port that follows the spec, and any MCP-capable AI can use it right away.'],
    [['usb', 'usb-c', 'socket', 'analogy', 'like what', 'plug', 'universal', 'metaphor'], 'The best analogy is <b>the USB-C of the AI world</b>: it used to be a hassle when every device had its own connector, but once USB-C standardized things, one cable connects everything. MCP does the same for AI and its tools and data—<b>one standard interface for all of them</b>.'],
    [['how it works', 'work', 'architecture', 'client', 'server', 'principle', 'design'], 'MCP is a <b>client–server architecture</b>: the <b>MCP Server</b> side wraps data or tools into a standard format and offers them; the AI app side acts as the <b>Client</b> that connects, requests data, or asks it to perform an action. Both sides talk over the same protocol, so it’s plug-and-play.'],
    [['server', 'server side', 'provider', 'which end', 'the server', 'host'], 'The <b>MCP Server</b> is the “provider”—for example a small program that connects to your database, files, or calendar. It exposes its capabilities per the MCP spec, so the AI can read data or issue commands through a standard interface.'],
    [['api', 'difference', 'different', 'compare', 'vs api', 'versus', 'how it differs'], 'An <b>API</b> is an interface each service defines on its own, so connecting to ten services means learning ten sets of rules. <b>MCP</b> is instead a <b>unified spec</b> that lets AI connect to every supporting service the same way—you could call it a <b>“standardized connection layer built for AI.”</b>'],
    [['rag', 'retrieval', 'vs rag', 'difference from rag', 'same as rag', 'compare'], 'They’re often used together but differ: <b>RAG</b> specifically solves “<b>go find relevant material in a knowledge base</b> before answering”; <b>MCP</b> is a broader <b>connection standard</b> that can reach data and also <b>use tools and perform actions</b>. RAG leans toward “looking things up”; MCP leans toward “plugging into everything.”'],
    [['agent', 'ai agent', 'relationship', 'vs agent', 'how it relates'], 'An <b>Agent</b> is an AI that plans and carries out goals on its own; <b>MCP</b> is the <b>standard socket</b> it reaches into when it needs a tool. The Agent decides “what to do,” and MCP handles “connecting to the tool in a uniform way to do it.”'],
    [['what can it do', 'application', 'use', 'example', 'tools', 'capabilities', 'can do'], 'Through MCP, AI can <b>read your files, query databases, search the web, work with code repositories, send messages</b>, and more. The key point: as long as a tool exposes a proper MCP port, <b>a different AI can use it just the same</b>, with no rewriting.'],
    [['safety', 'security', 'risk', 'privacy', 'downside', 'danger', 'permission', 'authorization'], 'MCP lets AI touch real data and tools, so <b>access control matters a lot</b>: which data to grant, and whether it can delete or modify—these boundaries must be set first. The price of convenience is that <b>the more you connect, the more careful you must be about permissions and misuse</b>.'],
    [['misconception', 'not', 'myth', 'think it is', 'replace', 'misunderstanding'], 'A common misconception: MCP <b>is not a model, and it is not meant to replace APIs</b>. It is simply <b>the standard spec used for connecting</b>—like defining the shape of a plug. The actual work is still done by the model and the tools behind it.'],
    [['who', 'where from', 'who made it', 'who created', 'anthropic', 'open', 'standard', 'free'], 'MCP is an <b>open standard introduced by Anthropic in 2024</b>, released for anyone to use. Because it’s open, anyone can build their own MCP Server, and different AI products can <b>share the same set of tools</b>.'],
  ],
  notes: [
    'MCP stands for Model Context Protocol, an open standard for connecting AI to tools and data.',
    'One-line analogy: MCP is the USB-C of the AI world—one way to connect to everything.',
    'Client–server architecture: the Server provides data/tools, and the AI side acts as the Client that connects and uses them.',
    'MCP leans toward “plugging into everything,” RAG toward “searching a knowledge base,” and the Agent is the brain that decides what to do.',
    'The flip side of convenience is risk: the more the AI connects to, the more careful you must be with permissions and authorization.',
  ],
};
