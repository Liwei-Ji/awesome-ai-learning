export default {
  qs: ['What is vector search?', 'Why does keyword search miss things?', 'How do you search millions of vectors fast?', 'What is hybrid retrieval?'],
  kb: [
    [['what is', 'definition', 'define', 'vector search', 'semantic search'], '<b>Vector search</b> means <b>finding data by meaning</b>: document passages are turned into vectors and stored, and at query time the question becomes a vector too, so the system returns <b>the passages closest in the space</b>. Close in meaning = close in distance, so even completely different wording can still be found.'],
    [['keyword', 'ctrl+f', 'miss', 'paraphrase', 'literal', 'why not'], 'Keyword search and Ctrl+F match the <b>literal text</b>: the document says “annual leave policy”, you ask about “days off”, and the words don’t line up, so nothing is found. <b>Passages that paraphrase</b> are exactly the blind spot of literal matching; vector search covers it with semantic distance.'],
    [['same space', 'question', 'query', 'embed', 'embedding model'], 'The key move is turning the question into a vector with the <b>same embedding model</b>, so the question and the passages land in <b>the same vector space</b>. Only in a shared space do distances mean anything; embed each side with a different model and the distances become meaningless.'],
    [['cosine', 'similarity', 'how to measure', 'distance', 'angle'], 'The usual measure is <b>cosine similarity</b>: it looks at the <b>angle between two vectors</b>, and the more aligned the directions, the closer the meaning and the higher the score. It cares only about direction, not length, which makes it a good fit for “how alike is the meaning”.'],
    [['top-k', 'topk', 'how many', 'nearest', 'k results'], '<b>Top-k</b> means keeping the k highest-similarity entries, so k=5 returns the 5 most relevant passages. Too small a k can miss the answer, too large a k pulls in noise and eats context, so in practice people often fetch a larger candidate set and let a <b>reranker</b> pick.'],
    [['ann', 'approximate nearest neighbor', 'hnsw', 'ivf', 'speed', 'too slow', 'scan'], 'Comparing against millions of vectors one by one is too slow. <b>Approximate nearest neighbor (ANN)</b> trades <b>a tiny bit of accuracy for huge speed</b>: <b>HNSW</b> hops along a graph toward the right neighborhood instead of scanning everything, and <b>IVF</b> sorts vectors into buckets and searches only the most likely few. The result isn’t guaranteed to be the absolute nearest, but it’s orders of magnitude faster.'],
    [['vector database', 'pgvector', 'faiss', 'pinecone', 'database', 'where stored'], 'A <b>vector database</b> is a system built to store vectors and serve fast similarity search, for example <b>pgvector</b> (adds vector search to PostgreSQL), <b>FAISS</b> (a retrieval library), and <b>Pinecone</b> (a managed cloud service). They package the ANN index for you, so you don’t reinvent the wheel.'],
    [['bm25', 'hybrid', 'which is better', 'compare', 'keyword versus'], '<b>Vectors catch meaning, keywords catch exact strings</b>: BM25-style keyword search is the most precise on terms that must match to the letter, while vector search is strongest on paraphrases. <b>Hybrid retrieval</b> runs both paths and merges the results, and in practice it’s more reliable than betting on either alone.'],
    [['rerank', 'reranker', 'reorder', 'refine', 'second stage'], '<b>Reranking</b> is the second stage: first vector or hybrid retrieval quickly pulls a batch of candidates (say 50 passages), then a finer model reads each one against the question and reorders them, keeping the best few. Coarse retrieval for speed, rerank for precision, this two-stage setup is the common architecture.'],
    [['weakness', 'fails', 'misses', 'code', 'model number', 'version number', 'name'], 'Vector search’s blind spot is <b>exact strings</b>: product codes, names, and version numbers (like v2.3.1) must match to the letter, and semantic vectors often blur them together. Those queries are safest with <b>keyword search</b>, which is the main reason hybrid retrieval exists.'],
    [['chunking', 'chunk', 'metadata', 'filter', 'cannot find'], 'Two things decide whether something can be found at all: first, <b>chunking quality</b>, since a badly cut passage can’t be surfaced whole no matter how good the search is; second, <b>metadata filters</b>, which narrow the candidate pool by date, team, or document type before the vector search runs, making it both faster and more accurate.'],
    [['embedding', 'rag', 'knowledge graph', 'relationship', 'which chapter', 'division'], 'The division of labor across three lessons: <b>Embedding</b> turns meaning into vectors (representation); <b>vector search</b> quickly finds the nearest few among millions (search); <b>RAG</b> takes the retrieved passages and generates the answer. And when a question needs multiple hops along relationships, that’s <b>knowledge graph</b> territory.'],
  ],
  notes: [
    'Vector search = finding by meaning: the question and the passages share one vector space, close in meaning = close in distance, take the nearest k.',
    'The usual measure is cosine similarity, “how aligned are the directions”, then keep the top-k.',
    'You can’t compare millions of vectors one by one: ANN (HNSW, IVF) trades a little accuracy for huge speed, and vector databases (pgvector, FAISS, Pinecone) package it up.',
    'Vectors catch meaning, keywords (BM25) catch exact strings: in practice, default to hybrid retrieval plus rerank.',
    'Chunking quality and metadata filters decide what can be found; exact codes and version numbers need keyword search.',
  ],
};
