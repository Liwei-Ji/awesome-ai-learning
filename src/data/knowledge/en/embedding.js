export default {
  qs: ['What is an embedding?', 'How does meaning turn into numbers?', 'What are embeddings used for?', 'How is Embedding different from a Tokenizer?'],
  kb: [
    [['what is', 'definition', 'define', 'embedding', 'word embedding', 'embed'], 'An <b>embedding</b> is what you get when you <b>turn a letter, word, or token into a string of numbers (a vector)</b>. Those numbers act as its “meaning coordinates,” letting a computer handle meaning through calculation.'],
    [['why', 'need', 'computer', 'doesnt understand words', 'numbers', 'purpose'], 'Because a <b>computer only understands numbers, not words</b>. The job of an embedding is to translate symbols into numbers, so the model has something to compute with later—without it, a neural network simply can’t take in text.'],
    [['how it works', 'principle', 'meaning', 'similar', 'close', 'semantic'], 'The core idea: <b>words with similar meanings sit close together as vectors</b>. <b>Cat</b> and <b>dog</b> land near each other, while <b>car</b> ends up far away. Those positional relationships are the meaning the model has learned.'],
    [['vector', 'coordinates', 'string of numbers', 'dimension', 'dimensions'], 'A <b>vector</b> is just a string of number coordinates, like [0.2, -0.7, 0.1 …]. Real models often have <b>hundreds to thousands of dimensions</b>; each dimension is like a hidden semantic feature, and together they describe the word.'],
    [['similarity', 'distance', 'how to compare', 'compare', 'cosine'], 'Once you have vectors, the <b>similarity between two words can be computed</b>—you look at <b>how close their vectors are and how aligned their directions are</b> (cosine similarity). Closer means more alike in meaning, and that is the basis of search, recommendation, and matching.'],
    [['example', 'king', 'queen', 'arithmetic', 'add subtract', 'analogy'], 'The most famous example: <b>king − man + woman ≈ queen</b>. You can add and subtract vectors, and the result reflects relationships like “gender” and “royalty”—proof that meaning really is encoded into the numbers.'],
    [['tokenizer', 'tokenize', 'difference', 'how are they different', 'order', 'relationship', 'vs'], 'The two form a <b>relay</b>: the <b>Tokenizer</b> first cuts text into tokens (chops it up), then the <b>Embedding</b> turns each token into a vector (gives it meaning). Cut first, convert second—only then does the Transformer take over to understand context.'],
    [['application', 'use', 'what for', 'where used', 'search', 'recommendation', 'clustering'], 'The uses are broad: <b>semantic search</b> (finding what means the same, not what matches letter for letter), <b>recommendation systems</b> (finding similar products or articles), <b>clustering and classification</b>, and <b>RAG</b> finding relevant material—all rely on embeddings.'],
    [['rag', 'retrieval', 'knowledge base', 'vector database', 'find data'], 'In <b>RAG</b>, documents are first turned into vectors and stored in a <b>vector database</b>. When you ask a question, the system turns the question into a vector too, <b>compares similarity</b> to find the most relevant passages, and hands those to the LLM to answer.'],
    [['misconception', 'not', 'dictionary lookup', 'same', 'compression', 'numbering'], 'A common misconception: an embedding <b>is not a dictionary lookup, nor is it giving each word an ID number</b>. An ID is just a label with no relationship to others; an embedding is a <b>learned coordinate</b>, where the numbers carry the meaning of “who is like whom.”'],
    [['where they come from', 'learned', 'training', 'origin', 'fixed'], 'These vectors are <b>not filled in by hand—the model learns them itself from massive amounts of text</b>: it relies on the pattern that “words that often appear together have similar meanings,” adjusting again and again so that similar words naturally drift closer.'],
    [['pros', 'cons', 'limits', 'limitations', 'advantages', 'disadvantages'], 'The upside: it turns meaning into <b>numbers you can compute with</b>, making similarity, search, and matching possible. The limits: a vector is <b>only as good as its training data</b>, any <b>bias in the data gets encoded in too</b>, and words with multiple meanings are hard to capture perfectly.'],
    [['images', 'sound', 'multimodal', 'text only', 'other'], 'Embeddings are <b>not just for text</b>: images, sound, and video can all be turned into vectors. This lets different types of data be <b>compared in the same space</b>, which is the key to reverse image search and cross-modal search.'],
  ],
  notes: [
    'An embedding = turning a word or token into a string of numbers (a vector) that represents its meaning coordinates.',
    'The core pattern: words with similar meanings sit close as vectors; very different meanings land far apart.',
    'The order of the pipeline: Tokenizer chops first → Embedding turns tokens into vectors → Transformer understands context.',
    'Once you have vectors, “similarity” can be computed directly from distance and direction—the basis of semantic search and RAG.',
    'Vectors are learned by the model from massive data, so any bias in that data gets encoded right into the vectors too.',
  ],
};
