export default {
  qs: ['What is a neural network?', 'What does “weight” mean?', 'Is a neural network the same as a human brain?', 'How is it different from deep learning?'],
  kb: [
    [['what is', 'definition', 'define', 'neural network', 'nn', 'meaning'], 'A <b>neural network</b> is a computing structure built from many <b>nodes (neurons)</b> linked together. It passes input signals <b>layer by layer, adds them up with weights, then squashes them</b>, and finally combines everything into a decision. The idea comes from the human brain, but at heart it’s a set of <b>math operations</b>.'],
    [['node', 'neuron', 'unit', 'cell', 'point'], 'A <b>node (neuron)</b> is the smallest computing unit in the network: it takes in the signals sent from the previous layer, <b>adds them up with weights</b>, decides <b>how brightly to light up</b>, and passes the result on to the next layer.'],
    [['weight', 'weights', 'connection', 'link', 'importance', 'strength'], 'A <b>weight</b> represents <b>how important the connection</b> between two nodes is. A big weight = this signal matters, listen closely; a small weight = nearly ignore it. Weights are the thing the network actually <b>learns and adjusts</b>.'],
    [['positive weight', 'negative weight', 'positive', 'negative', 'support', 'against', 'sign'], 'Weights <b>can be positive or negative</b>: a <b>positive weight = support</b> (when this clue appears, lean more toward a certain answer); a <b>negative weight = opposition</b> (when this clue appears, push a certain answer down instead). This tug-of-war between positive and negative lets the network make subtle judgments.'],
    [['activation', 'activate', 'lit up', 'brightness', 'excited', 'fire'], 'An <b>activation</b> is <b>how brightly a node is lit up</b>, usually a number between 0 and 1. The closer to 1, the more “excited” the node is, the more sure it is that the feature it watches for really showed up.'],
    [['how it works', 'how does it work', 'process', 'flow', 'signal', 'how it computes', 'pass through'], 'The flow is: the input layer takes in the data → each node <b>multiplies its incoming signals by weights and adds them all up</b> → passes through a <b>squash</b> that turns it into an activation between 0 and 1 → sends it to the next layer, on and on, and <b>the final layer</b> is the answer.'],
    [['layer', 'layers', 'structure', 'input layer', 'hidden layer', 'output layer', 'how many layers'], 'A neural network usually has three kinds of layer: the <b>input layer</b> (takes in the raw data), <b>hidden layers</b> (pull out clues step by step in the middle, there can be many), and the <b>output layer</b> (gives the final answer). More layers = the more complex the patterns it can express.'],
    [['squash', 'activation function', 'why', 'sigmoid', 'relu', 'function', 'nonlinear', 'curve'], 'After a node adds everything up, it passes through an <b>activation function</b> that <b>squashes</b> the number into a range (say 0 to 1). This step is crucial: it lets the network learn <b>curved, nonlinear</b> patterns, instead of just adding signals up as-is.'],
    [['human brain', 'brain', 'same', 'like', 'real', 'biological', 'think', 'conscious'], 'A neural network <b>takes its inspiration</b> from the brain’s neurons, “take in signals → fire once you hit a threshold”, but it <b>isn’t really a brain</b>. It has no consciousness and doesn’t think; it just uses <b>math</b> to imitate the idea of “connections and their strengths.”'],
    [['deep learning', 'difference', 'relationship', 'different', 'same as', 'vs'], 'The two are inseparable: <b>deep learning is just a neural network with “many layers.”</b> A neural network is the basic structure; stack it deep and large and let it pull out features layer by layer on its own, and you get deep learning.'],
    [['learn', 'how does it know', 'where do weights come from', 'training', 'adjust', 'learning', 'at first'], 'At the start the weights are set <b>randomly</b>, so the network just guesses wildly. Then through <b>training</b> (the gradient descent and backpropagation covered in later chapters), it <b>adjusts the weights bit by bit</b> based on how wrong each guess was, until it gets things right more often.'],
    [['downside', 'upside', 'black box', 'hard to understand', 'application', 'where is it used', 'explain', 'bias'], 'Neural networks can automatically learn <b>complex patterns humans could never write out</b> (recognizing faces, speech, translation), and every node also has a <b>bias</b> that acts as its firing threshold. The downside is that it acts like a <b>black box</b>: the weights are just a pile of numbers, so it’s hard to explain “why” it decided the way it did.'],
  ],
  notes: [
    'A neural network = a computing structure of many nodes strung together by weighted connections, inspired by the human brain.',
    'A weight represents the importance of a connection; it can be positive (support) or negative (opposition), and it’s what actually gets adjusted during training.',
    'An activation is how brightly a node is lit up (about 0-1); signals are weighted, summed, and squashed layer by layer until they become a single decision.',
    'It’s split into input, hidden, and output layers; stack a neural network very deep and you get deep learning.',
    'It imitates the brain’s idea of “connections,” but it has no consciousness and doesn’t think, at heart it’s just math.',
  ],
};
