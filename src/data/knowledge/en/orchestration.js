export default {
  qs: ['What is agent orchestration?', 'Why use a graph instead of one big agent?', 'What are the common orchestration patterns?', 'When should I use orchestration, and when not?'],
  kb: [
    [['definition', 'what is', 'define', 'orchestration', 'orchestrate'], '<b>Agent orchestration</b> is what you do when a task is too big or too branchy for one agent loop: you design the system as a <b>graph</b> and decide how the work moves through it (routing, branching, looping, handoffs). Orchestration = <b>deciding how work flows between multiple steps or agents</b>.'],
    [['graph', 'node', 'edge', 'nodes', 'edges', 'draw a graph'], 'The graph is simple: a <b>node</b> is a step or a sub-agent, and an <b>edge</b> is the flow between nodes. Drawing the system as nodes plus edges lets you see exactly what runs first, where it goes next, and where it branches, instead of hiding it all inside one black box.'],
    [['single agent', 'one agent', 'workflow', 'hard-coded', 'linear', 'compare', 'difference'], 'Contrast three approaches: a <b>single agent</b> is one think, act, observe loop that decides everything itself, flexible but prone to wandering and hard to control; a <b>hard-coded linear workflow</b> (A→B→C) is always the same, predictable but rigid; an <b>orchestration graph</b> is the middle ground, explicit nodes and edges with <b>conditional routing</b>, so you get structure and flexibility at once.'],
    [['router', 'dispatcher', 'route', 'branch', 'decide where'], 'A <b>router (dispatcher)</b> is the most common node: it inspects the input and sends the work down the right branch. For example, an incoming support message is classified as a refund, a technical issue, or a general question, each taking a different downstream path.'],
    [['orchestrator', 'worker', 'orchestrator-worker', 'lead agent', 'delegate', 'subtasks'], 'In the <b>orchestrator-worker</b> pattern, a lead agent breaks a goal into subtasks, <b>delegates each one to a worker agent</b>, and then gathers the results. It is like a team lead handing pieces of the job to specialists.'],
    [['parallel', 'fan-out', 'gather', 'concurrent', 'at once', 'merge'], '<b>Parallel fan-out then gather</b>: run several nodes at the same time (say, query three sources at once), then <b>merge</b> their outputs into one result. It saves time and suits subtasks that don’t depend on each other.'],
    [['loop', 'iterate', 'repeat', 'guard', 'max iterations', 'infinite'], 'A <b>loop</b> node repeats until a condition is met (for example, “revise until it passes the check”). You must add a <b>guard or max iterations</b>, or it will spin forever, burning money and stalling. This ties back to loop control.'],
    [['handoff', 'hand off', 'transfer', 'specialist', 'pass control'], 'A <b>handoff</b> is when one agent passes <b>control and context</b> to a specialist agent that takes over. Think of front-line support that can’t resolve an issue transferring the whole conversation to a technical expert.'],
    [['human', 'human-in-the-loop', 'approval', 'pause', 'review', 'sign-off'], '<b>Human-in-the-loop</b>: a node <b>pauses and waits for a person to approve</b> before continuing. Use it in front of irreversible or high-risk actions (sending email, making a payment, writing to production) so a human checks first.'],
    [['state', 'shared state', 'intermediate', 'resumable', 'debuggable'], 'The graph carries a <b>shared state</b> between nodes: what has been done and what intermediate results came back. Explicit state is what makes the system <b>debuggable and resumable</b> (it can pick up after an interruption) instead of forgetting everything at each step.'],
    [['why', 'benefit', 'control', 'reliability', 'observability', 'reusability', 'why a graph'], 'Why a graph instead of one giant prompt or one big agent? Four benefits: <b>controllability</b> (you can see and constrain the flow), <b>reliability</b> (each node is testable and failures are isolated), <b>observability</b> (you can trace which path ran), and <b>reusability</b> (nodes compose into other graphs). Frameworks like <b>LangGraph</b> make this concrete, as one example, not something to over-focus on.'],
    [['when', 'when to use', 'do not use', 'pitfall', 'downside', 'over-engineering', 'cost'], 'Use orchestration for <b>multi-step tasks with branching, loops, or multiple specialists</b>; for a <b>single simple call, don’t wrap it in a graph</b>. Common pitfalls: <b>over-engineering</b> (a graph where one call would do), <b>state bloat</b>, <b>infinite loops</b> (which need guards), and too many hops adding latency and cost.'],
  ],
  notes: [
    'When a task is too big or too branchy, draw the system as a graph: nodes are steps or sub-agents, edges are the flow.',
    'Orchestration decides how work moves through the graph: routing, branching, parallel, loops, handoffs, human review.',
    'The graph carries shared state, making the system debuggable and resumable; every loop needs a guard so it can’t run forever.',
    'A graph buys you control, reliability, observability, and reusability, beating one giant prompt.',
    'The agent is the reasoning loop, tools and MCP are its hands, skills are its playbooks, and orchestration is the wiring diagram that connects them; don’t over-engineer a simple task.',
  ],
};
