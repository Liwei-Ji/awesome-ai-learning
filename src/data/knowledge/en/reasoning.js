export default {
  qs: ["What is a reasoning model?", "How does it differ from a standard LLM?", "Why does thinking longer make it more accurate?", "Should I use it on every problem?"],
  kb: [
    [["what is", "reasoning model", "o1", "what is a reasoning model", "reasoning"], "A <b>reasoning model</b> is an LLM that thinks first and then answers. Before answering it produces a long stretch of internal thinking: it breaks the problem down, checks, backtracks, tries different paths, and only then gives an answer. Models like o1 are the representatives. Compared with a standard model, it trades for higher accuracy on hard problems, at the cost of being slower and more expensive."],
    [["difference", "vs standard", "standard llm", "comparison", "different"], "The difference is <b>whether it thinks first</b>: a standard LLM starts spitting out its answer word by word the moment it finishes reading the question; a reasoning model spends a stretch of thinking before answering. In practice, a standard model relies on you using a <b>CoT prompt</b> to force it to think, while a reasoning model uses <b>post-training</b> to build the ability to think into the model itself."],
    [["cot", "chain of thought", "step by step", "prompt", "steps"], "<b>Chain of Thought (CoT)</b> is a <b>prompt technique</b>: in the prompt you ask the model to think step by step and write out its intermediate reasoning, which raises accuracy on hard problems because the problem gets broken into small steps. The key point is that CoT relies on <b>you asking for it in your input</b>, and the model itself has not changed; a reasoning model, by contrast, has this behavior <b>trained in</b>."],
    [["test-time", "test-time compute", "think longer", "more accurate", "trade-off", "why accurate"], "The core is <b>test-time compute</b>: at answer time you let the model think a few more steps and generate more thinking token. The longer it thinks, the better it does on hard problems, but also the slower and more expensive. At its core it is not getting smarter but <b>trading compute for accuracy</b>, so it only pays off on hard problems."],
    [["cost", "expensive", "slow", "latency", "token", "money"], "A reasoning model's thinking token still <b>costs money and takes time</b>: a single problem may generate several times the token, so both cost and latency rise noticeably. So do not turn it on by default; use a standard model for simple tasks and save long thinking for problems that are genuinely hard and demand accuracy."],
    [["thinking process", "is it real", "explanation", "trustworthy", "trace", "internal"], "Be careful: the thinking process the model prints out is <b>not necessarily its real internal computation</b>; it is more like an explanation produced after the fact to show you. It has reference value, but <b>do not treat it as a reliable explanation or basis for compliance audit</b>, and it may look reasonable yet lead to the wrong answer."],
    [["when to use", "should i", "choose", "suitable", "every problem"], "You should not use it on every problem. <b>Worth long thinking</b>: multi-step reasoning, math, code, planning, and cases that need the highest accuracy. <b>Not worth it</b>: general Q&A, extraction, rewriting, and everyday tasks that want to be fast and cheap, where a standard model is fast and good enough. For a compromise, use a <b>standard model + CoT prompt</b>."],
    [["relationship", "fine-tuning", "rl", "reinforcement learning", "how it is trained", "post-training"], "A reasoning model's ability to think is produced by <b>post-training</b>: after pretraining and instruction fine-tuning, <b>reinforcement learning</b> is used to reward the process of thinking clearly and answering correctly. So it comes after fine-tuning: pretraining learns language → fine-tuning learns to follow instructions → reasoning training learns to think first and then answer."],
  ],
  notes: [
    "A standard LLM answers as soon as it finishes reading; a reasoning model first produces a long stretch of thinking, checks and backtracks, and only then answers.",
    "CoT is a prompt technique (you ask it to think); a reasoning model uses post-training to build the ability to think into the model.",
    "The core trade-off is test-time compute: the longer it thinks the more accurate, but the slower and more expensive.",
    "It is not getting smarter; it just spends more compute to think a few more steps, and it will still make mistakes and hallucinate.",
    "The printed thinking process is not necessarily the real internal computation, so do not treat it as a reliable explanation.",
    "Only hard problems are worth using it for; for everyday tasks a standard model is fast and good enough.",
  ],
};
