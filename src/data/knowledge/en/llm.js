export default {
  qs: ['How does ChatGPT generate its answers?', 'Is a large language model looking things up in a database?', 'Why do I get a different answer each time I ask the same question?', 'Why does an LLM state wrong things so confidently?'],
  kb: [
    [['what is', 'definition', 'define', 'llm', 'large language model', 'language model'], 'A <b>large language model (LLM)</b> is an AI trained on a huge amount of text, and its core ability is really just one thing: <b>predicting the next token (a character or word)</b>. Repeat that one move over and over, and it can write a whole fluent passage.'],
    [['how it works', 'how it generates', 'how it produces', 'mechanism', 'next token', 'predict next', 'one word at a time'], 'At every step an LLM does the same thing: it reads all the text so far, <b>computes the probability of the next word</b>, picks one to add on, then feeds it back into the input and computes the next. <b>One word after another</b>, a whole answer takes shape.'],
    [['look up', 'search', 'find the answer', 'database', 'google', 'query', 'search engine'], 'A common misconception is that it’s <b>looking things up in a database or searching the web</b>. It isn’t—it’s <b>guessing the most plausible next word on the fly</b>, so answers are “generated,” not “looked up.” That’s also why it sometimes makes things up.'],
    [['understand', 'know', 'think', 'conscious', 'really understand', 'does it understand'], 'An LLM <b>doesn’t truly understand what you mean, and has no consciousness</b>. It relies on statistical patterns to compute “what usually comes after this kind of phrasing.” It looks like understanding, but at heart it’s <b>extremely good text autocompletion</b>.'],
    [['difference', 'differ', 'regular program', 'traditional', 'vs programming'], 'Regular software has its rules hard-coded; an LLM <b>has no one writing each rule for it</b>—instead it generalizes them itself from a huge amount of text. So it can answer questions you never set up in advance, but for the same reason it’s <b>not guaranteed to be right every time</b>.'],
    [['large', 'why called large', 'how big', 'parameters', 'size', 'scale'], 'The “large” refers to a <b>very large number of parameters</b> (often billions to hundreds of billions of weights), plus a huge amount of training text. The bigger the scale, the more complex the language patterns it can capture and the more fluent its answers—but also <b>more expensive and slower</b>.'],
    [['random', 'different each time', 'not the same', 'probability', 'temperature', 'creative', 'why different'], 'At each step it computes <b>a probability for a whole row of candidate words</b>, then picks with a bit of randomness, so <b>the same question comes out slightly different each time</b>. Turning up the “temperature” makes it more creative but more prone to wandering; turning it down makes it steadier and more conservative.'],
    [['token', 'word', 'unit', 'smallest unit', 'a single word'], 'A <b>token</b> is the smallest unit an LLM processes text in, roughly one character or one word (see the “Tokenizer” chapter for details). From start to finish an LLM is doing the same one thing: <b>predicting the next token</b>.'],
    [['transformer', 'attention', 'relationship', 'architecture', 'based on', 'engine'], 'An LLM’s internal engine is usually a <b>Transformer</b> (see the previous chapter): it uses <b>Attention</b> to track how the context connects, which is how it figures out who “he” refers to and what the whole sentence is about. With that grasp of context (still statistical association, not real understanding), predicting the next word becomes accurate.'],
    [['application', 'use', 'what can it do', 'example', 'where is it used', 'features'], 'An LLM can <b>write, translate, summarize, code, answer questions, and chat</b>, because all of these boil down to “what should come next.” The ChatGPT, AI customer service, and writing assistants you use are all powered by it underneath.'],
    [['hallucination', 'wrong', 'makes things up', 'fabricate', 'trustworthy', 'accurate', 'confidently wrong'], 'Because it “guesses the most plausible word” rather than “looking up facts,” it will <b>confidently make things up (hallucinate)</b>, and <b>no matter how confident it is, it can still be wrong</b>. Always verify important information yourself, or pair it with RAG so it looks up sources before answering.'],
    [['chatgpt', 'gpt', 'claude', 'gemini', 'which ones', 'which models', 'products', 'examples'], 'Common LLM products include <b>ChatGPT, Claude, and Gemini</b>. Their core principle is the same—they all <b>keep predicting the next token</b>—the differences lie in training data, scale, and how they’re fine-tuned.'],
  ],
  notes: [
    'An LLM’s core is just one thing: keep predicting the next token, growing an answer one word at a time.',
    'It “generates” answers rather than “looking them up”—which is why it’s both fluent and prone to making things up.',
    'At each step it computes a probability for a whole row of candidate words, then picks with a bit of randomness, so each answer differs slightly.',
    '“Large” = many parameters and a lot of training text; bigger is usually stronger, but also more expensive and slower.',
    'An LLM is built on top of the Transformer, using Attention to read the context, then predicting the next word.',
  ],
};
