export default {
  qs: ['What exactly is a Skill?', 'How is a Skill different from tools or MCP?', 'How is a Skill different from fine-tuning?', 'Why do we need Skills?'],
  kb: [
    [['what is', 'definition', 'define', 'skill', 'skills', 'meaning'], 'A <b>Skill</b> is a self-contained folder: a set of instructions plus optional scripts, templates, and reference files that teaches an AI agent how to do one specific task well. It is loaded only when the task calls for it; otherwise it costs just its name and a short description.'],
    [['why', 'need', 'solve', 'problem', 'benefit', 'purpose', 'point of it'], 'Without skills, teaching an agent to do something means either stuffing it into the system prompt (always taking up context) or retraining (expensive and slow). Skills package the know-how as <b>files loaded only when needed</b>: reusable and maintainable, so a team can turn its SOPs and playbooks into a bundle the agent uses.'],
    [['progressive disclosure', 'on demand', 'load', 'how it works', 'mechanism', 'principle'], 'The key mechanism is <b>progressive disclosure</b>: the agent normally sees only each skill’s <b>name and a one-line description</b>, and reads the full instructions and bundled files only when it judges the skill relevant. That is why you can give it hundreds of skills without blowing up the context.'],
    [['tools', 'mcp', 'tool', 'difference', 'different', 'vs tools', 'vs mcp'], '<b>Tools and MCP</b> are the agent’s hands: they connect to outside systems, read data, call APIs, take actions. A <b>skill</b> is the playbook: what steps to follow, what format to produce, how to do the job well. A skill can itself call tools. Tools give the <b>ability to act</b>; skills give the <b>know-how to act well</b>.'],
    [['fine-tuning', 'fine tune', 'finetune', 'training', 'vs fine-tuning'], '<b>Fine-tuning</b> bakes behavior into the weights: expensive, static, and it needs retraining to change. A <b>skill</b> is just files loaded at runtime: cheap, editable, shareable, and version-controllable, like RAG for procedures. To change a capability you swap a folder of files, without touching the model.'],
    [['system prompt', 'vs system prompt', 'difference from system prompt'], 'A <b>system prompt</b> is a role and rules that stay in context the whole time, always present; a <b>skill</b> is <b>loaded only when relevant</b> and can be long and detailed. Put simply: the system prompt covers who you are and the global rules, while a skill covers how to do one specific task well, step by step.'],
    [['description', 'name', 'metadata', 'how to write', 'trigger', 'triggering'], 'A skill’s <b>description</b> matters most, because that is what the agent uses to decide whether to load it. Write a crisp description that says clearly when the skill should be used; if it is vague, the agent will miss it or load the wrong one.'],
    [['contents', 'what is in it', 'files', 'structure', 'made of', 'inside'], 'A skill usually contains: <b>metadata (name and description, always visible)</b>, the full instructions, and optional scripts, templates, and reference files. Only the metadata stays in front of the agent; everything else is <b>loaded only when needed</b>, keeping the always-on footprint tiny.'],
    [['composable', 'portable', 'share', 'sharing', 'version', 'library'], 'Skills are <b>composable</b>: one agent can hold many and use whichever fits. They are also <b>portable and shareable</b>: a skill is just a folder, so you can version it, hand it to someone, or drop it into a skill library.'],
    [['example', 'examples', 'use case', 'application', 'what can it do'], 'Examples: turn your <b>slide formatting rules</b>, a <b>financial-report analysis procedure</b>, a <b>PR review checklist</b>, or the <b>correct way to use a specific API</b> each into a skill. Teams package their SOPs and playbooks as skills, and the agent loads whichever one the task needs.'],
    [['safety', 'security', 'risk', 'danger', 'untrusted', 'malicious'], 'A skill can carry <b>instructions and scripts</b>, so <b>treat it like code</b>: do not install skills of unknown origin, just as you would not run a random executable. Read what is inside before installing, and only use trusted sources.'],
    [['misconception', 'not', 'myth', 'one job', 'one thing', 'common mistake'], 'A common misconception: a skill <b>is not a model or a magic plug-in</b>, just a packaged bundle of know-how. Another rule is <b>one skill, one job</b>: rather than cramming ten tasks into one, split them into small, precise skills so the agent picks the right one, and <b>verify it actually triggers the right skill</b>.'],
  ],
  notes: [
    'A skill is a folder of instructions plus optional scripts and templates that teaches an agent to do one task well.',
    'Progressive disclosure: only the name and description are shown by default; full content loads on demand, so hundreds fit without blowing up the context.',
    'Tools and MCP give the ability to act; skills give the know-how to act well, and a skill can call tools.',
    'No retraining: a skill is files loaded at runtime, so it is cheap, editable, shareable, and version-controllable.',
    'Treat skills like code: they can carry instructions and scripts, so do not install ones you do not trust.',
  ],
};
