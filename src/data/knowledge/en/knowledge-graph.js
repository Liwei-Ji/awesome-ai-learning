export default {
  qs: ['What is a knowledge graph?', 'How is GraphRAG different from plain RAG?', 'When should I use a knowledge graph?', 'How is a knowledge graph built?'],
  kb: [
    [['what is', 'definition', 'define', 'knowledge graph', 'triple', 'triples', 'entity', 'relationship'], 'A <b>knowledge graph</b> stores knowledge as a network: <b>entities are nodes</b> and <b>relationships are edges</b>. Each fact is a <b>triple</b>: <b>subject → relation → object</b>, for example “Anthropic → created → Claude.” In one line: it records not chunks of text but <b>who relates to whom, and how</b>.'],
    [['graphrag', 'graph rag', 'difference', 'how is it different', 'vs rag', 'versus rag', 'compare', 'not the same'], 'Plain RAG searches <b>text</b>: it finds chunks whose wording is close to the question. <b>GraphRAG</b> searches <b>relationships</b>: it follows edges across the graph to reach the answer. The difference is the retrieval method, <b>matching keywords vs tracing connections</b>, which is why GraphRAG can answer questions that no single chunk holds.'],
    [['multi-hop', 'multihop', 'multi hop', 'across documents', 'connect', 'reasoning', 'chain', 'killer use case'], '<b>Multi-hop</b> reasoning is its killer use case: the answer sits in <b>no single passage</b> but is scattered across documents and only surfaces once you connect several facts. Take “which of our suppliers are also investors in a competitor?” Flat RAG stalls because no one chunk holds the answer; a graph traces the chain the whole way.'],
    [['local', 'global', 'local search', 'global search', 'community', 'cluster', 'theme', 'summarize'], 'There are two flavors of query. <b>Local search</b> asks about a <b>specific entity and its neighborhood</b>, such as “who does this supplier deal with.” <b>Global search</b> asks about <b>themes or patterns</b> across the whole dataset, such as “what big topics run through these documents,” by grouping nodes into <b>communities</b> and summarizing each cluster.'],
    [['how to build', 'build', 'extraction', 'entity extraction', 'relationship extraction', 'pipeline', 'construct', 'create'], 'Building a graph goes roughly: <b>① entity extraction</b> (pull out the things) → <b>② relationship extraction</b> (how they connect) → <b>③ define a schema</b> (the types) → <b>④ dedup and normalization</b> (merge duplicate nodes into one) → <b>⑤ add confidence scores</b>. Extraction usually has an LLM read the documents and emit triples one by one.'],
    [['schema', 'types', 'structure', 'design', 'ontology', 'spec'], 'The <b>schema</b> is the graph’s spec: it spells out <b>which entity types (person, company, product…) and relation types (invests-in, supplies, part-of…)</b> exist. With it, extraction has a standard and nodes don’t sprawl. <b>A fuzzy schema yields a messy, untrustworthy graph</b>, so nailing it down first is crucial.'],
    [['dedup', 'deduplication', 'normalization', 'normalize', 'merge', 'same node', 'alias', 'canonical'], '<b>Dedup and normalization</b> merge the different ways of writing the same thing into <b>one canonical node</b>: for example “Anthropic” and “Anthropic PBC” are the same company and must become one node. Otherwise the graph grows two phantom entities, the relationships get split in half, and multi-hop paths can’t be traced through.'],
    [['query', 'cypher', 'sparql', 'query language', 'traversal', 'how to query', 'ask'], 'There are two ways to query: one is a <b>graph query language</b> (<b>Cypher, SPARQL</b>) that describes the path to walk directly; the other lets an <b>LLM translate your natural-language question into a graph traversal</b> that follows edges to the answer. Both mean “walk along the relationships,” not match the wording.'],
    [['benefits', 'pros', 'accuracy', 'tokens', 'save', 'hallucination', 'why', 'results'], 'Four benefits: higher <b>multi-hop accuracy</b> (about <b>18 percent</b> over baseline RAG in reported figures), <b>far fewer tokens</b> (you retrieve a focused subgraph, not dozens of chunks), <b>fewer hallucinations</b> (relationships are explicit, not vibes), and it can answer <b>global questions</b> flat RAG cannot.'],
    [['quality', 'model size', 'small model', 'big model', 'key insight', 'matters', 'important'], 'The key insight: <b>graph quality matters more than model size</b>. <b>A good graph with a small model often beats a poor graph with a big model.</b> Rather than paying for a fancier model, make the <b>build steps</b>, extraction, dedup, and schema, solid; that usually lifts results more.'],
    [['trade-off', 'tradeoff', 'cost', 'cons', 'when to use', 'when', 'should i', 'maintenance', 'effort', 'combine'], 'The trade-off: <b>building and maintaining the graph is more work than dumping chunks into a vector database</b>, and any source change forces re-extraction and re-merging. So <b>use it for relational or multi-hop questions, and skip it for simple lookup</b>. In practice it’s often <b>combined</b> with plain RAG: the graph for multi-hop and global questions, vector search for keywords and detail.'],
    [['example', 'application', 'use case', 'scenario', 'engineering', 'due diligence', 'concrete'], 'Two concrete examples: an <b>engineering work graph</b>, joining GitHub, Jira, and Linear so you can ask “which PRs and tickets does this bug touch”; and <b>due diligence</b>, joining reports, founders, investors, legal cases, and subsidiaries so hidden links between parties jump out at a glance.'],
  ],
  notes: [
    'A knowledge graph stores knowledge as entities (nodes) and relationships (edges); each fact is a triple: subject → relation → object.',
    'Plain RAG searches text and matches keywords; GraphRAG searches relationships and follows edges, so it shines on multi-hop questions whose answer spans many documents.',
    'Building = entity extraction + relationship extraction + schema + dedup and normalization; querying uses Cypher/SPARQL or an LLM that translates the question into a graph traversal.',
    'Benefits: higher multi-hop accuracy (about +18%), far fewer tokens, fewer hallucinations, and it can answer global questions.',
    'Graph quality matters more than model size; building is more work, so use it for relational/multi-hop questions and often combine it with plain RAG.',
  ],
};
