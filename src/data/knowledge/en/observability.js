export default {
  qs: ['How do I monitor an AI product after launch?', 'What is tracing?', 'What is drift?', 'How do I avoid breaking the AI in production?'],
  kb: [
    [['observability', 'what is', 'monitoring', 'traditional monitoring', 'difference', 'observe'], '<b>Observability</b> means making a system’s internal state visible: keep records, chain traces, compute metrics. Traditional monitoring asks “is it down?” (errors, crashes, timeouts); AI observability adds another layer: <b>has the quality of the answers gotten worse</b>, because a broken AI usually throws no error.'],
    [['no error', 'silent', 'silently', 'why needed', 'broken', 'gets worse', 'fails quietly'], 'Because AI failures are <b>quiet</b>: a model swap, a prompt tweak, or user questions changing shape, and it keeps producing output, just worse, without throwing anything. Traditional software fails loudly; AI fails silently, so <b>launch day is the day monitoring starts</b>. Running without observation is driving with your eyes closed.'],
    [['logging', 'log', 'what to log', 'fields', 'record', 'keep records'], 'For every call, record: <b>input, output, model version, prompt version, token usage and cost, latency, and which tools were called</b>. The version fields matter most: when quality drops, the first question is always “what changed that day?”, and without versions in the log you cannot answer it.'],
    [['trace', 'tracing', 'replay', 'multi-step', 'chain', 'one request'], 'A <b>trace</b> chains every step of one request (input → retrieval → prompt assembly → generation → tool calls → output) into <b>one replayable trail</b>. Multi-step flows like agents and RAG need this most: when something goes wrong you can replay what it saw and what it did, instead of staring at just the final wrong answer.'],
    [['quality metrics', 'metric', 'metrics', 'what to measure', 'latency', 'cost', 'refusal'], 'System metrics (latency, error rate, cost) are table stakes; AI-specific <b>quality metrics</b> matter more: whether answers include citations, the refusal rate, the share of thumbs-down, the average of sampled scores. All system metrics green while quality metrics slide is exactly how AI products most often break.'],
    [['drift', 'distribution', 'shift', 'how to detect', 'slowly worse', 'over time'], '<b>Drift</b> means inputs or quality quietly shifting over time: users start asking new kinds of questions (input drift), or quality slowly declines (quality drift), with no error anywhere. The way to catch it is to <b>watch trends, not single points</b>: track the long-run curves of input distribution and quality scores against the baseline from early after launch.'],
    [['llm-as-judge', 'judge', 'online scoring', 'sampling', 'who scores', 'live quality'], 'Live quality relies on <b>sampling plus LLM-as-judge</b>: you cannot read every response, so sample a share and let another model score them against your standards, and alert when the score moves. But the judge has biases and drifts too, so <b>periodically spot-check the judge’s scores by hand</b> to keep it calibrated, otherwise you are monitoring with a broken thermometer.'],
    [['regression test', 'regression', 'test set', 'break it', 'model swap', 'prompt edit'], 'A <b>regression test set</b> is a fixed set of questions you run before shipping any change (model swap, prompt edit, retrieval change), comparing scores before and after. The best questions come from <b>past production failures</b>: every pothole you hit goes into the set, so you never hit the same one twice.'],
    [['alert', 'alerts', 'threshold', 'when to alarm', 'alerting'], 'Set alerts on <b>changes</b>: quality score falling below baseline, refusal or thumbs-down rate suddenly rising, cost or latency jumping abnormally, input distribution clearly shifting. The knack is to alert on trend changes, not on a single bad answer, otherwise alert fatigue sets in and soon nobody looks.'],
    [['human', 'human review', 'people', 'who reviews', 'role of humans'], 'Humans are not replaced, they are <b>placed where they count</b>: machines handle mass sampling and first-pass screening, humans review the <b>flagged samples</b> (low scores, thumbs-down, cases the judge was unsure about) and periodically audit the judge itself. Spending limited human attention on the most suspicious samples is what makes it scale.'],
    [['privacy', 'pii', 'personal data', 'mask', 'masking', 'sensitive'], 'Logs often contain users’ <b>personal data (PII)</b>: names, phone numbers, addresses, account IDs. <b>Mask or de-identify before storing</b>, and set retention limits and access controls. Observability is for watching the system, not the users; a leaked log is deadlier than a quality dip.'],
    [['relationship', 'evaluation', 'evaluation chapter', 'which chapter', 'before launch', 'division'], 'This chapter and the previous one, <b>AI Evaluation</b>, are a relay: evaluation covers <b>before launch</b> (benchmarks, blind tests, confirming it is good enough to ship), observability covers <b>after launch</b> (seeing, measuring, catching degradation). Evaluation is the entrance exam, observability is the health checkup; passing the exam does not mean you never get sick.'],
  ],
  notes: [
    'A broken AI throws no error: it keeps answering, just worse, so launch day is when monitoring starts.',
    'Log every call: input, output, model and prompt versions, tokens, latency; chain multi-step runs into one trace.',
    'System metrics are table stakes; AI quality metrics (citations, refusals, thumbs-down) and drift matter more.',
    'Live quality = sampling plus LLM-as-judge, alerting on trends; audit the judge itself by hand periodically.',
    'Run a regression test set (built from past failures) before every change, roll out gradually; prompt edits count as deployments.',
  ],
};
