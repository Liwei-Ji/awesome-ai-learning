export default {
  qs: ['What does backpropagation do?', 'Why “backward”?', 'What is the chain rule?', 'What is a gradient?'],
  kb: [
    [['backprop', 'backpropagation', 'what is', 'definition', 'define', 'meaning'], 'In one line: <b>backpropagation</b> is an algorithm that takes the blame for “how wrong the answer was” and <b>hands it out layer by layer, from back to front</b>, to every weight. It works out the direction and size of the adjustment each weight needs, and it’s the core step in training a neural network.'],
    [['how it works', 'process', 'steps', 'flow', 'how', 'work'], 'It runs in two passes: <b>①forward pass</b>—data travels from start to end and produces a prediction and its error; <b>②backward pass</b>—the error then travels back from end to start, handing blame to each weight. Forward finds the answer, backward finds “what to change.”'],
    [['backward', 'why backward', 'reverse', 'back to front', 'go back', 'backwards'], 'Because blame has to be <b>traced back from the result</b>: first you know how wrong the final output was, then you can work out how much the layer before it contributed, then the layer before that… like chasing a problem up a supply chain—you have to <b>trace from the finished product back upstream</b>.'],
    [['chain rule', 'chain', 'calculus', 'math', 'derivative', 'rule'], 'The <b>chain rule</b> is a tool from calculus: when a result is a function “nested” layer inside layer, it lets you <b>break the overall change into the change at each layer, multiplied together</b>. Backpropagation relies on it to compute, precisely and layer by layer, each weight’s share of the blame.'],
    [['gradient', 'slope', 'direction', 'effect', 'influence', 'gradients'], 'A <b>gradient</b> is “if this weight moves a tiny bit, does the error grow or shrink, and by how much.” What backpropagation actually computes is the gradient for each weight—it tells us both <b>which direction to adjust</b> and <b>how hard to adjust</b>.'],
    [['gradient descent', 'training', 'relationship', 'together', 'work with', 'difference'], 'The two <b>divide the labor</b>: <b>backpropagation</b> “computes” the gradient for each weight; <b>gradient descent</b> then “uses those gradients” to nudge the weights one step in the direction that shrinks the error. Compute, take a step, repeat—and the model keeps getting more accurate.'],
    [['weight', 'weights', 'adjust', 'update', 'change', 'tune'], 'Training is all about tuning <b>weights</b> (the strength of the connections). Backpropagation’s job is to work out, for <b>every single weight</b>, “how much blame you carry for this mistake,” so the next step knows how much to adjust each connection.'],
    [['important', 'why important', 'deep', 'feasible', 'deep learning', 'significance', 'key'], 'Without backpropagation, the thousands upon thousands of weights in a deep network <b>simply couldn’t be trained efficiently</b>. It’s exactly what made “multi-layer networks that learn features on their own” feasible—you could call it <b>the key that let deep learning take off</b>.'],
    [['analogy', 'example', 'like what', 'plain', 'everyday', 'simple'], 'Imagine a team that just lost a game: the <b>forward</b> pass is playing the match and seeing the score (the error); the <b>backward</b> pass is the coach reviewing it afterward, <b>handing blame to each player</b> and telling them what to fix. Next game (the next round), everyone improves a little.'],
    [['misconception', 'myth', 'not', 'wrong idea', 'common mistake', 'misunderstanding'], 'A common misconception: thinking backpropagation “learns on its own.” In fact it <b>only computes the blame (the gradients)</b>—it doesn’t decide how far to move; that’s the job of the <b>learning rate</b> and gradient descent. It’s a computing tool, not the whole of training.'],
    [['vanishing', 'exploding', 'problem', 'challenge', 'downside', 'too many layers', 'flaw'], 'As blame is multiplied back layer by layer, if every layer multiplies by a very small number, by the time it reaches the front it’s <b>close to 0 (vanishing gradients)</b> and the early layers barely learn; if it multiplies by a very large number, it <b>explodes</b>. This is a classic difficulty in training deep networks.'],
    [['forward', 'difference', 'forward pass', 'contrast', 'two passes', 'front and back'], '<b>Forward pass</b>: front to back, aimed at “computing the answer and the error.” <b>Backward pass</b>: back to front, aimed at “computing each weight’s share of the blame.” One pass produces the answer, the other finds the direction to improve—you can’t do without either.'],
  ],
  notes: [
    'Backpropagation = first run forward to get the answer, then go back to front handing the error’s blame to each weight.',
    'The “blame” it computes is the gradient: it tells each weight which way to adjust and by how much.',
    'The chain rule is the math behind it: breaking the overall error into the change at each layer, multiplied together.',
    'Backpropagation “computes the gradients,” gradient descent “takes a step”—it takes both together to train.',
    'With too many layers, gradients easily vanish or explode—a classic difficulty for deep networks.',
  ],
};
