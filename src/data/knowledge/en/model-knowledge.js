export default {
  qs: ["Where is a model's knowledge stored?", "What is a knowledge cutoff?", "Why does a model misremember?", "How do you fill in when its knowledge is not enough?"],
  kb: [
    [["where stored", "where", "parameters", "weights", "how is it stored", "stored where", "location"], "A model's knowledge is <b>spread across billions of parameters (weights)</b>, not stored as a lookup database. During pretraining it read through enormous amounts of text and compressed recurring patterns and facts into the weights; when answering, it rebuilds that knowledge by <b>predicting the next word</b>."],
    [["cutoff", "knowledge cutoff", "how far", "latest", "up to date", "outdated", "when", "stops at"], "The <b>knowledge cutoff</b> is the endpoint of the training data in time. Once training stops, knowledge <b>freezes</b> at that moment, and anything that happens afterward is something the model has not read, so it does not know it. Do not fully trust it on the \"latest\" news, and verify important time-sensitive information separately."],
    [["misremember", "incomplete memory", "hallucination", "why wrong", "inaccurate", "from impression", "makes things up"], "Because it <b>rebuilds knowledge from impression</b>, not by looking up a table. Knowledge is spread across the weights, with no single cell explicitly storing a given fact, so it <b>can be incomplete or wrong</b>, and it still speaks with confidence. The more specific the number, date, or quotation, the easier it is to get wrong."],
    [["how to fill in", "not enough", "new knowledge", "supplement", "external", "supply"], "There are three ways: <b>①</b> paste it <b>into the context</b> (put it in the prompt: fastest for a one-off); <b>②</b> <b>RAG</b> (real-time retrieval from an external knowledge base, can cite sources, good for facts that change often or are private); <b>③</b> <b>fine-tuning</b> (writes tone and format into the weights, but is not suited for loading in facts)."],
    [["fine-tuning", "fine tuning", "finetune", "load in knowledge", "cram in facts", "why not"], "<b>Fine-tuning is not the tool for loading in facts</b>: it mainly changes <b>behavior, tone, and format</b>, it is costly and hard to update, and it often fails to retain new facts. For facts that change or are private, <b>RAG is more reliable</b>, because the data lives externally and can be swapped at any time."],
    [["parametric knowledge", "parametric", "internal knowledge", "innate", "knows by default"], "<b>Parametric knowledge</b> refers to knowledge compressed into the weights and rebuilt through generation, the part the model \"knows by default.\" By contrast, what comes through RAG or pasting into the prompt is <b>external knowledge</b>, provided temporarily and not retained afterward. The two can work together: the foundation comes from parameters, while freshness and privacy come from external sources."],
    [["database", "lookup table", "search", "is it like", "difference", "search engine"], "A model is <b>neither a database nor a search engine</b>. A database looks up an exact record; a model kneads knowledge into the weights and generates from impression. This lets it <b>generalize from one case to many</b>, but it can also <b>be fuzzy and make things up</b>. When you need precision or traceable sources, you usually have to attach external retrieval (RAG)."],
    [["private", "company", "internal", "personal", "confidential", "my own data"], "A model has <b>never read your private data</b> (company documents, personal records), so it cannot answer from the start. The best approach is to put this data into a <b>knowledge base and retrieve it with RAG</b>, feeding it to the model only when needed. This keeps things updatable and controllable, without the risk of fine-tuning on it or sending it out."],
  ],
  notes: [
    "A model's knowledge is compressed into parameters (weights) during pretraining; it is not stored as a lookup database.",
    "When answering, it rebuilds knowledge by \"predicting the next word,\" more like speaking from impression than looking up a table.",
    "Knowledge has a cutoff: anything after training, and private data, the model simply does not know.",
    "A model sounding confident does not mean it remembers correctly; specific numbers, dates, and quotations are the easiest to get wrong.",
    "There are three ways to add new knowledge: paste into the context, RAG, and fine-tuning.",
    "For facts that change often or are private, RAG is usually more reliable than fine-tuning.",
  ],
};
