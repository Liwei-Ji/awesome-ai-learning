export default {
  qs: ['How is inference different from training?', 'Why do AI services charge money?', 'Does chatting with AI make it smarter?', 'How do you make inference faster and cheaper?'],
  kb: [
    [['definition', 'what is', 'define', 'inference', 'meaning'], 'In one line: <b>inference is taking a trained model and actually putting it to use</b>—you give it an input, it computes an output. The model learns the rules during training; at inference time it simply <b>applies</b> those rules to produce an answer.'],
    [['training', 'difference', 'differ', 'vs', 'compare', 'versus', 'contrast', 'same'], 'These are the model’s two phases: <b>training</b> is a <b>one-time</b> job of learning the rules (slow, expensive, changes the weights); <b>inference</b> is applying those rules every time you use it (fast, but every use means recomputing from scratch). Training is like studying; inference is like answering the exam.'],
    [['conversation', 'chat', 'chatgpt', 'each time', 'use', 'ask', 'reply', 'message'], 'Every time you send ChatGPT a message and wait for its reply, <b>you’ve triggered one inference</b>. The model reads your input and computes the answer one word at a time—that whole process is inference.'],
    [['cost', 'charge', 'expensive', 'fee', 'why pay', 'billing', 'money', 'pricing'], 'Because <b>every inference actually spends real compute</b> to run, and the bigger the model and the longer the answer, the more it costs. That’s why AI services mostly <b>bill by usage (number of tokens)</b> rather than as a one-off purchase.'],
    [['latency', 'speed', 'slow', 'fast', 'wait', 'lag', 'response time', 'delay'], '<b>Latency</b> is the waiting time from when you hit send to when you start seeing the answer. It’s affected by model size, input length, and server load—a bigger model is usually slower, which is the common price you pay for “smarter.”'],
    [['learn', 'remember', 'get smarter', 'update', 'misconception', 'train me', 'does it learn'], 'A common misconception: that chatting with AI teaches it things and it gets smarter the more you talk. In reality <b>the model doesn’t change its weights at all during inference</b>—it just applies rules that were fixed long ago. To actually make it stronger, you have to <b>retrain or fine-tune</b> it.'],
    [['where it runs', 'cloud', 'local', 'gpu', 'chip', 'hardware', 'on device', 'device'], 'Inference can run on a <b>cloud server</b> (often on GPUs, good for large models) or <b>locally on your own device</b> (phone, laptop—good for small models and better for privacy). The more compute you have, the faster inference goes.'],
    [['faster', 'optimize', 'cheaper', 'quantization', 'distillation', 'speed up', 'save', 'efficient'], 'Three common tricks: <b>quantization</b> (use lower-precision numbers for more speed and savings), <b>distillation</b> (train a small model to imitate a big one), and <b>batching</b> (compute many requests together). The goal is always to get a good-enough answer with less compute.'],
    [['token', 'word by word', 'one word', 'llm', 'generate', 'how computed', 'step by step'], 'For a language model, one inference is really <b>doing many tiny steps over and over</b>: each step predicts the next token, appends it, then predicts the next… so <b>the longer the answer, the more steps to compute</b> and the more time it takes.'],
    [['batch', 'batching', 'together', 'at once', 'concurrent', 'group requests'], '<b>Batching</b> means taking many users’ requests and <b>computing them together at the same time</b>, fully using the GPU so the average cost per inference drops. It’s one of the keys to how cloud AI services keep costs low.'],
    [['precision', 'accuracy', 'does quantization hurt', 'impact', 'quality', 'trade-off'], 'Quantization uses coarser numbers, <b>but usually loses only a tiny bit of accuracy</b> in exchange for clearly faster and cheaper inference—well worth it in most situations. It’s a “speed vs. quality” trade-off, not black and white.'],
    [['model size', 'bigger is slower', 'parameters', 'relationship', 'large model'], 'The bigger the model (more parameters), the more it has to compute per inference—<b>usually smarter, but also slower and more expensive</b>. So in practice people often pick a model that’s “just good enough” rather than always chasing the biggest.'],
  ],
  notes: [
    'Training = learning the rules (slow, once); inference = using the rules (fast, recomputed every time).',
    'Every single conversation you have with an AI is one inference.',
    'During inference the model doesn’t change its weights—chatting won’t make it smarter; that takes retraining.',
    'Latency: the waiting time from input to output; bigger models are usually slower.',
    'Quantization, distillation, and batching all make inference faster and cheaper on compute.',
  ],
};
