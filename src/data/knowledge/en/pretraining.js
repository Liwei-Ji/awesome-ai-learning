export default {
  qs: ['How is ChatGPT actually trained?', 'What is pretraining?', 'What does RLHF do?', 'Why can’t a raw model just chat with you?'],
  kb: [
    [['what is', 'this chapter', 'definition', 'model to chatgpt', 'overview', 'concept'], 'This chapter is about how a raw model that can only <b>ramble on and continue text</b> gets shaped, step by step, into an <b>assistant that follows instructions and holds a conversation</b>. It happens in three stages: <b>pretraining → instruction tuning → RLHF</b>.'],
    [['three stages', 'flow', 'steps', 'order', 'pipeline', 'how trained', 'how it’s built', 'which three'], 'Training ChatGPT takes three steps: <b>①pretraining</b>, read a huge amount of text to learn language; <b>②instruction tuning (SFT)</b>, teach it to answer the way humans want; <b>③RLHF</b>, use human preferences to tune its tone and safety. Each step is smaller than the last, and each brings it closer to being a “useful assistant.”'],
    [['pretraining', 'pretrain', 'massive reading', 'first step', 'first stage', 'read the internet'], '<b>Pretraining</b> is the first step and the most expensive one: the model reads in <b>nearly the entire internet’s</b> worth of text, with a single task, <b>guess the next word</b>. No human has to label the answers; just by reading, it soaks up language, common sense, and writing style.'],
    [['unsupervised', 'self-supervised', 'need labels', 'no labels', 'no labeled answers', 'do you label'], 'Pretraining is <b>unsupervised (self-supervised)</b>: nobody hand-labels each sentence. You just take ordinary text, hide the words that come next, and let the model guess them. <b>The correct answer is the original text itself</b>, which is why it can swallow such enormous amounts of data.'],
    [['instruction tuning', 'sft', 'second step', 'teach it to obey', 'follow instructions', 'supervised finetuning'], '<b>Instruction tuning (SFT)</b> is the second step: feed the model lots of <b>“question → good answer”</b> demonstrations so it learns “oh, humans want me to answer the question, not just continue writing it.” This step is <b>supervised</b>, and uses far less data than pretraining.'],
    [['rlhf', 'human feedback', 'reinforcement learning', 'third step', 'alignment step', 'preferences', 'reward'], '<b>RLHF (Reinforcement Learning from Human Feedback)</b> is the third step: people <b>rank and compare</b> several of the model’s answers, that data trains a “<b>reward model</b>” to score answers, and the score then guides the AI toward replies that are more liked and safer. This step makes answers more <b>useful and on-target</b>, and also more polite and safe.'],
    [['raw model', 'base model', 'foundation model', 'can’t use directly', 'can’t chat', 'why finetune', 'difference'], 'A model that has only been pretrained is a <b>raw (base) model</b>. Ask it “what is the capital of Taiwan” and it might reply “what is the capital of Taiwan, what is the capital of Japan…”, because it only learned to <b>continue text</b>, not to <b>answer</b>. It takes the next two steps to turn it into a conversational assistant.'],
    [['pretraining vs finetuning', 'pretraining and finetuning', 'what’s the difference', 'compare', 'how do they differ'], '<b>Pretraining</b>: learn language itself from scratch, huge data, no labels, very expensive. <b>Fine-tuning</b>: make small adjustments on top of an existing model, little data, labeled, far cheaper. One is <b>laying the foundation</b>; the other is <b>the interior decorating</b>.'],
    [['reward model', 'how it scores', 'ranking', 'rating', 'judge'], 'The <b>reward model</b> is RLHF’s judge: first humans rank several answers from best to worst, and those preferences train a model that <b>scores answers the way humans would</b>. After that, every answer the AI produces gets scored and nudged by it, no human has to watch over it constantly.'],
    [['alignment', 'safety', 'harmful', 'refuse', 'won’t make things up', 'values'], '<b>Alignment</b> means getting the model’s behavior to match what humans expect, help when it should help, refuse when it should refuse. RLHF is the main way alignment is achieved: it teaches the model to be <b>polite, to not teach harmful things, and to admit when it doesn’t know</b>.'],
    [['why not pretrain yourself', 'cost', 'expensive', 'ordinary person', 'can a company do it', 'compute'], 'Pretraining needs <b>a huge amount of data plus astronomical compute</b>, so the cost is enormous, only big companies can afford it. That’s why almost everyone else <b>stands on the shoulders of giants</b>: take a model someone already pretrained and only do the cheap fine-tuning (see the “Fine-tuning” chapter).'],
    [['relationship', 'previous chapters', 'llm', 'training', 'gradient descent', 'how this connects'], 'This chapter ties the earlier ones together: the model’s knack for “guessing the next word” comes from the <b>large language models</b> chapter; every weight adjustment relies on <b>gradient descent and backpropagation</b>; and here we cover <b>what data and how many stages</b> are used to assemble those techniques into an assistant you can actually use.'],
    [['instruction tuning vs rlhf', 'sft vs rlhf', 'second step third step difference', 'how the two differ'], '<b>Instruction tuning</b> teaches the model “<b>what to do</b>”, it learns to answer questions from demonstrations; <b>RLHF</b> teaches the model “<b>how to do it better</b>”, it learns from human preferences to answer more thoughtfully and safely. The first gives a standard answer; the second gives a better-vs-worse comparison.'],
  ],
  notes: [
    'Going from model to ChatGPT takes three stages: pretraining → instruction tuning (SFT) → RLHF.',
    'Pretraining is unsupervised, large-scale reading whose only task is “guess the next word”, the most expensive and most crucial step.',
    'A model that has only been pretrained can only continue text, not answer questions, so it can’t serve as an assistant on its own.',
    'RLHF uses human preferences to train a reward model, making answers more useful and on-target, and also more polite and safe.',
    'Getting a model’s behavior to match human expectations is called “alignment”, the key to how useful and how safe an assistant is.',
  ],
};
